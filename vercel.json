{
  "installCommand": "if [ -d frontend ]; then npm install --prefix frontend; else npm install --prefix ../frontend; fi",
  "buildCommand": "if [ -d frontend ]; then npm run build --prefix frontend && rm -rf dist && cp -R frontend/dist dist; else npm run build --prefix ../frontend && rm -rf dist && cp -R ../frontend/dist dist; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
